#include "monty.h"

/**
 * nop -  does nothing
 * @stack: inconsequential
 * @line_count : for nothing
 *
 * Return: NOTHING
 */
void nop(stack_t **stack, unsigned int line_count)
{
	(void) stack;
	(void) line_count;
}
